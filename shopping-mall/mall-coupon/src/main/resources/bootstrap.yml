spring:
  application:
    name: mall-coupon
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  config:
    import:
      # 引入数据信息
      - nacos:${spring.application.name}?group=DEFAULT_GROUP
      - nacos:${spring.application.name}-all?group=DEFAULT_GROUP&refresh=true