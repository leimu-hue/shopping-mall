spring:
  application:
    name: mall-admin
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  config:
    import:
      - nacos:${spring.application.name}?group=DEFAULT_GROUP
